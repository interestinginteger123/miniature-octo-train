name: aws-py-eks
runtime:
  name: python
  options:
    virtualenv: venv
description: A minimal AWS Python EKS for the user
template:
  config:
    aws:region:
      description: The AWS region to deploy into
      default: eu-west-2